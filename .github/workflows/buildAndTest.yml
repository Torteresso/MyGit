name: BuildAndTest

on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout sources
        uses: actions/checkout@v6
      - name: Build with Gradle and test
        run: ./gradlew build